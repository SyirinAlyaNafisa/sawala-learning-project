// FILE SUMBER EXPORT

//----- Named Export -----
export function tambah(a, b) {
  return a + b;
}

// ----- Default export (cuma boleh 1 per file) -----
export default function kali(a,b){
  return a* b;
}