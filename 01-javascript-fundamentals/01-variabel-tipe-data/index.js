// materi variabel & tipe data

//1. variabel (tempat menyimpan data)
// const : tidak bisa berubah
const nama = "syirin  alya nafisa";

//let : variabel yang bisa berubah nantinya
let umur = 20;
umur = 21; // bisa berubah karena pakai let

console.log("Nama:", nama); // browser devtools alat bawaan browser yang digunakan untuk melihat, mengecek, dan membantu memperbaiki website.
console.log("Umur:", umur); // console.log(): untuk menampilkan nilai/data di console supaya kita bisa melihat apakah kode kita berjalan sesuai yang diharapkan.

// 2. Tipe data dasar (jenis data yag di simpan di variabel)

const string = "Hello friend!"; //teks
const number = 28; //angka
const boolean = true; //  true or false
const array = ["mangga", "jeruk", "apel"]; //kumpulan data/list
const object = { nama: "budi", umur: 23, alamat: "swiss" }; //key value
const contohNull = null;
let undefined;

//return adalah perintah untuk mengembalikan hasil dari sebuah function.
